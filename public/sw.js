// No-op service worker. The app registers no offline/caching behavior — this
// file exists only so browser installability checks (which probe /sw.js when
// a manifest.json is present) get a 200 instead of a 404 in the console/logs.
